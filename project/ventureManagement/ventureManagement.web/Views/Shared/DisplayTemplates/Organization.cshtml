@model VentureManagement.Models.Organization

@{
    var title = ViewData.ContainsKey("title") ? ViewData["title"].ToString() : "";
    var itemId = ViewData.ContainsKey("itemId") ? ViewData["itemId"].ToString() : "";
    var detail = ViewData.ContainsKey("detail");
    var url = ViewData.ContainsKey("url") ? ViewData["url"].ToString() : "";
}

@(
 Html.X().FormPanel()
        .ItemID(itemId)
        .Title(title)
        .Collapsible(!detail)
        .Collapsed(!detail)
        .Url(url)
        .DefaultAnchor("100%")
        .StyleSpec("margin-bottom:5px;")
        .Frame(true)
        .Items(
            Html.X().HiddenFor(o=>o.OrganizationId),
            Html.X().TextFieldFor(o=>o.OrganizationName).ReadOnly(true),
            Html.X().TextFieldFor(o=>o.Description).ReadOnly(true)
        )
)
